# 旧规则解析备份（不启用）

- `parser.py`：集成 LLM 前的 Scope V2 关键词、模块、日期和接口映射逻辑。
- `legacy.py`：更早的 AI Index topic/domain 默认映射。

服务入口、ServerProfile 和新意图模块不导入本目录，也不会在模型失败时回退到这里。
部分历史回归测试显式导入旧解析器，以验证存量采集计划的兼容行为。
备份范围是被替换的输入解析；原有分析、写作和结果校验流程继续使用。
